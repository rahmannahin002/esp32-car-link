plugins {
    id 'com.android.application'
}

android {
    compileSdk 35 // Ensure this is high enough for Android 16/15 APIs

    defaultConfig {
        applicationId "com.example.esp32controller"
        minSdk 24
        targetSdk 35 
        versionCode 1
        versionName "1.0"
    }
}

dependencies {
    implementation 'androidx.appcompat:appcompat:1.6.1'
}